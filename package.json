{
  "name": "flowqueue-fake-api",
  "version": "1.0.0",
  "description": "Fake API for FlowQueue",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}